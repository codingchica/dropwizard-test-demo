/**
 * Internal representations of the domain objects. These may contain modifications that are needed
 * internally for processing/business logic, but should not necessarily be provided by / returned to
 * the client.
 *
 * <p>Alternatively, there may be some fields that are only conditionally populated on the response,
 * depending upon access levels of the client calling the API.
 */
package codingchica.demo.test.dropwizard.core.model.mappers;
